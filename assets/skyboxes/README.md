Drop `.dds` skybox textures here (or in `~/Documents/LeagueDirector/skyboxes`).

Riot’s official skyboxes are not redistributed. Copy them from a League Director
Python install (`resources/skyboxes/*.dds`) if you have one.
